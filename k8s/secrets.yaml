# Template — fill values and apply with: kubectl apply -f secrets.yaml
# For production, use sealed-secrets or external-secrets-operator instead.
apiVersion: v1
kind: Secret
metadata:
  name: paseo-secrets
  namespace: paseo
type: Opaque
stringData:
  PASEO_PASSWORD: "" # bcrypt hash of daemon password
  # ANTHROPIC_API_KEY: ""
  # OPENAI_API_KEY: ""
  # SENTRY_DSN: ""
  # PASEO_AUDIT_HMAC_SECRET: ""
  # PASEO_ROLE_ADMIN: "" # bcrypt hash
  # PASEO_ROLE_OPERATOR: "" # bcrypt hash
  # PASEO_ROLE_VIEWER: "" # bcrypt hash
